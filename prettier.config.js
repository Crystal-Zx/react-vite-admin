export default {
  printWidth: 80, // 单行代码最大宽度（单位：字符），默认值 80
  tabWidth: 2, // tab 宽度（单位：字符），默认值 2
  useTabs: false, // 不使用 tab 制表符代替空格，默认值 false
  trailingComma: 'none', // 不使用尾随逗号，默认值 'none'，可选值有 'none' | 'es5' | 'all'
  semi: false, // 不使用尾随分号，默认值 true
  singleQuote: true, // 使用单引号
  quoteProps: 'as-needed', // 属性名仅当需要时是否使用引号
  jsxSingleQuote: true, // JSX中使用单引号
  bracketSpacing: true, // 对象字面量的括号间是否加空格，默认值 true，可选值是true（加空格）或false（不加空格）
  jsxBracketSameLine: false, // 在JSX中的 '>' 符号另起一行
  arrowParens: 'avoid' // 箭头函数仅一个参数时不添加括号
}
